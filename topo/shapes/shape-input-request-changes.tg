shape shape_input_request_changes {
  name "Request Changes Input"
  description "Review note required when requesting changes."

  fields {
    submission_id uuid required
    reviewer_note text required
  }

  status active
}
