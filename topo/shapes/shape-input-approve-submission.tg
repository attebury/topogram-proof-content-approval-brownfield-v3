shape shape_input_approve_submission {
  name "Approve Submission Input"
  description "Review note captured when approving a submission."

  fields {
    submission_id uuid required
    reviewer_note text optional
  }

  status active
}
